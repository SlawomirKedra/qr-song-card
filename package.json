{
  "name": "qr-song-cards",
  "private": true,
  "description": "Prosta aplikacja do generowania i eksportu kart z kodami QR dla piosenek (React + Express).",
  "scripts": {
    "postinstall": "npm --prefix client install && npm --prefix server install",
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "license": "MIT"
}
