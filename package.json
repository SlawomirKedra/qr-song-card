{
  "name": "qr-song-cards",
  "private": true,
  "description": "Karty QR z piosenkami (React + Express). Front/back, Spotify playlist import.",
  "scripts": {
    "postinstall": "npm --prefix client install && npm --prefix server install",
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "license": "MIT"
}
